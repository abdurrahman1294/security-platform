# V4.1 Dependency Resolution and Resumable Autonomy

V4.1 resolves safe prerequisites before stopping, creates explicit operator queues for sensitive prerequisites, supports resumable missions, isolates state by engagement output, and preserves authorization/scope/registered-entrypoint governance.
